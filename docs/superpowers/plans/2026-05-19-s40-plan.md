# S40 plan — gecko-yield-verdict to publish-ready + the benchmark

**Date:** 2026-05-19 · **Premise:** S37's 6/6 oracle is **deployed and live**
(verified — `/trade_research` returns the post-#99 envelope). The
`gecko-yield-verdict` skill is built (#127), envelope-corrected (#130), and
verified end-to-end live (Step 1 onchainOS discovery ✓, Step 2 verdict ✓);
#132 fixed the `vertical` bug (canon was silently dropped). Backtest Phase 0
is built (#131). S40 takes the skill to publish-ready and turns the backtest
into the trust benchmark.

## State going in

- Live: 6/6 oracle deployed; `gecko-yield-verdict` data+verdict path verified.
- Branches: `s38/gecko-yield-verdict` (skill + strategy docs), `s39/backtest-phase0` (#131).
- Founder-side, open: OKX MCP is on a personal real-funds account — needs a
  sub-account isolation before any OKX *execution*. The onchainOS CLI is on
  `gecko-okx-context` (agent account) — fine.

## Workstreams

| WS | Scope | Gating |
|---|---|---|
| **WS1 — skill completion** | gecko-yield-verdict: Step 3 render verified against a live verdict; the demo run. | Founder-gated — needs the OKX sub-account isolation (MCP) for the demo. |
| **WS2 — backtest Phase 1** | `as_of_date <= T` retrieval gate on the trade-panel `$match` (additive: optional `as_of` param, no-op when unset) + a Pattern-F end-to-end leakage probe. | **Overnight-executable** — money-safe, OKX-free. |
| **WS3 — backtest Phase 2** | `reconstruction.py` — DeFiLlama/Pyth historical fetch, truncate-at-T, cache. | After WS2. |
| **WS4 — publishing** | Path A MR prep for `gecko-yield-verdict` (security-scan conformance, `_meta.json`, README). | After WS1 smoke-verified. |

Carry-over backlog: #92 (drift wrong-instrument), #91 (berkshire mojibake),
#100 (pooling contract test), #104 (`make test-fast` OOM — supervised only).

## Overnight autonomous run (2026-05-19, founder asleep)

**Mandate:** "plan the next sprint and execute." **Executing only WS2** —
the one workstream that is money-safe, OKX-free, and founder-action-free.
Everything else is founder-gated or sequenced after.

**Hard boundaries:** no merge to main · no deploy · no X402 flip · no
`okx-agent-trade-kit` MCP / no OKX execution / no money · no main-push or
force-push · feature branches only · bounded LLM spend · sequential code
agents (no parallel-code git tangle) · never a bare `uv run pytest` to a
background agent.

## Sequencing

WS2 runs tonight → WS3 follows once the `as_of` gate + probe land → WS1 + WS4
resume when the founder is back and the OKX sub-account is isolated. The
benchmark (the trust instrument) is the through-line: Phase 0 (done) → Phase 1
(tonight) → Phase 2 → contamination-controlled cycle.
